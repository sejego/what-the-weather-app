function generateAPIKey() {
  // Unfortunately, it is not possible to hide  the API key in
  // Github Pages. Ideally, this should not be exposed, but since this is a simple app
  // to showcase JS, HTML and CSS skills, this can be here.
  const API_KEY = "6af93c0daeb0749c5631a37be0ef4bc8";
  return API_KEY;
}

export { generateAPIKey };
